import NotFoundComponent from '../components/NotFound'
import { withLayout, MinimalLayout } from '../components/layout'

export const NotFound = withLayout(MinimalLayout)(NotFoundComponent)

export default NotFound
